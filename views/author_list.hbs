<h1>{{title}}</h1>

<ul>
    {{#each author_list as |author|}}
    <li>
        <a href={{author.url}}> {{author.name}} </a> ({{author.lifespan}})
    </li>
    {{else}}
    <li>There are no authors</li>
    {{/each}}

</ul>