<h1>Genre: {{genre.name}}</h1>

<div style="margin-left:20px; magin-top:20px;">

    <h4>Books</h4>

    <dl>
        {{#each genre_books as |book| }}
        <dt>
            <a href={{book.url}}>{{book.title}}</a>
        <dd>{{book.summary}}</dd>
        </dt>
        {{else}}
        <p>This genre has no books</p>
        {{/each}}
    </dl>

</div>